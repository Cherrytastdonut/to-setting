 git config --global user.name "user name"     
 git config --global user.email "user email"


If you want to use VScode with github,
It may be a problem caused by the connection between the github id and the VScode when it is cloned in github but not committed. In such a situation, I share the terminal code that can connect the github and VScode
